# THIS FILE IS AUTOMATICALLY GENERATED BY CARGO
#
# When uploading crates to the registry Cargo will automatically
# "normalize" Cargo.toml files for maximal compatibility
# with all versions of Cargo and also rewrite `path` dependencies
# to registry (e.g., crates.io) dependencies.
#
# If you are reading this file be aware that the original Cargo.toml
# will likely look very different (and much more reasonable).
# See Cargo.toml.orig for the original contents.

[package]
edition = "2024"
rust-version = "1.92.0"
name = "s3s-aws"
version = "0.13.0-s4.1"
publish = false
build = false
autolib = false
autobins = false
autoexamples = false
autotests = false
autobenches = false
description = "S3 service adapter integrated with aws-sdk-s3 (vendored fork for s4 NoVerify mode)"
readme = "README.md"
keywords = ["s3"]
categories = [
    "web-programming",
    "web-programming::http-server",
]
license = "Apache-2.0"
repository = "https://github.com/Nugine/s3s"

[features]
minio = ["s3s/minio"]

[lib]
name = "s3s_aws"
path = "src/lib.rs"

[dependencies.async-trait]
version = "0.1.89"

# default-features = false drops the legacy `rustls` feature (EOL rustls 0.21
# chain, RUSTSEC-2026-0098/0099/0104); see workspace Cargo.toml.
[dependencies.aws-sdk-s3]
version = "1.124.0"
default-features = false
features = [
    "sigv4a",
    "http-1x",
    "default-https-client",
    "rt-tokio",
]

[dependencies.aws-smithy-runtime-api]
version = "1.11.5"
features = [
    "client",
    "http-1x",
]

[dependencies.aws-smithy-types]
version = "1.4.5"
features = ["http-body-1-x"]

[dependencies.aws-smithy-types-convert]
version = "0.60.13"
features = ["convert-time"]

[dependencies.hyper]
version = "1.8.1"

[dependencies.s3s]
version = "0.13.0-s4.1"
path = "../s3s"
default-features = false

[dependencies.std-next]
version = "0.1.9"

[dependencies.sync_wrapper]
version = "1.0.2"

[dependencies.tracing]
version = "0.1.44"

[dependencies.transform-stream]
version = "0.3.1"

[lints.clippy]
dbg_macro = "warn"
module_name_repetitions = "allow"
multiple_crate_versions = "allow"
self_named_module_files = "deny"
# v0.13.0-s4.1 vendored fork: cargo-metadata lints are workspace-scoped,
# silence them so we don't police every workspace member from the
# vendored copy.
cargo_common_metadata = "allow"
negative_feature_names = "allow"
redundant_feature_names = "allow"
wildcard_dependencies = "allow"
# Silence pedantic/new lints — see crates/s3s.
doc_markdown = "allow"
must_use_candidate = "allow"
semicolon_if_nothing_returned = "allow"
too_many_lines = "allow"
items_after_statements = "allow"
unnecessary_wraps = "allow"
needless_pass_by_value = "allow"
missing_errors_doc = "allow"
missing_panics_doc = "allow"
cast_possible_truncation = "allow"
cast_sign_loss = "allow"
cast_lossless = "allow"
unreadable_literal = "allow"
too_many_arguments = "allow"
ignored_unit_patterns = "allow"
elidable_lifetime_names = "allow"
unnecessary_semicolon = "allow"

[lints.clippy.all]
level = "deny"
priority = -1

[lints.clippy.cargo]
level = "deny"
priority = -1

# v0.13.0-s4.1: same relaxation as crates/s3s — see that file's comment.
[lints.clippy.pedantic]
level = "warn"
priority = -1

[lints.rust]
unsafe_code = "forbid"
